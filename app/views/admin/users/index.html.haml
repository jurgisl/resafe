%h1 Users

%table.table
  %tr
    %th Username
    %th Last login
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td
        = user.last_sign_in_at.strftime('%d.%m.%Y %H:%M:%S') if !user.last_sign_in_at.nil?
      %td= link_to 'Show', [:admin, user]
      %td= link_to 'Edit', edit_admin_user_path(user)
      %td= link_to 'Destroy', [:admin, user], method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User', new_admin_user_path, :class => "btn"
