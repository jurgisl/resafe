%h1 Listing groups

%table.table
  %tr
    %th Name
    %th
    %th
    %th

  - @groups.each do |group|
    %tr
      %td= group.name
      %td= link_to 'Show', [:admin, group]
      %td= link_to 'Edit', edit_admin_group_path(group)
      %td= link_to 'Destroy', [:admin, group], method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Group', new_admin_group_path
